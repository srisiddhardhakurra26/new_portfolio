# Frontend API base (optional). If set, client will call `${REACT_APP_API_URL}/api/*`.
# Recommended to point to your Vercel deployment so local dev hits production API.
# Example:
# REACT_APP_API_URL=https://sid-portfolio-api.vercel.app

# Server-side key used by server.js when running locally (Express) or in Vercel if applicable.
# Note: Using REACT_APP_ prefix exposes variables to the client build; keep this key server-only in production.
REACT_APP_GROQ_API_KEY=your_groq_api_key_here
